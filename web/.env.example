# Environment Variables
# Copy this file to .env.local for local development

# API base URL
# In production (static export), this should point to where FastAPI is running
NEXT_PUBLIC_API_URL=http://localhost:8000
